Must-have-Security-Token: 
  description: API Must have Security Token
  oas: Both
  severity: critical
  enabled: true
  rule: 
      Path: '$.paths.[*].security.[*]'
      type: match
      options: 
        match: 
