Must-have-Security-Token: 
  description: API Must have Security Token
  oas: all
  severity: critical
  enabled: true
  rule: 
      Path: '$.paths.[*].security.[*]'
      type: shouldExists
      options: 
        match: 
