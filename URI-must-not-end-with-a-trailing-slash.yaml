rule name: 
  description: "A URI\_must not\_end with a trailing slash (/)."
  oas: Both
  severity: critical
  enabled: true
  rule: 
    Path: $.paths.*~
    type: match
    options: 
      match: '[^\/]$'