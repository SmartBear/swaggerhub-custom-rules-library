Hal-JSON-1: 
  description: "All\_response\_messages\_MUST\_support an\_application/hal+json\_(HAL) format.   (1 of 2)"
  oas: Both
  severity: critical
  enabled: true
  rule: 
    Path: 'paths.*.[put,post,get,delete,patch,options].produces'
    type: shouldExists
    options: 
      match:
